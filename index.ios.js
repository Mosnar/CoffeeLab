/**
 * CoffeeLab App
 * Ransom Roberson
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';
import Root from './app/root';

// Register root view
AppRegistry.registerComponent('coffeelab', () => Root);
